# Parity report — keyboard-shortcuts mounting + tracker arbitration (2026-08-26)

Scope: Agent B wave mounting the input-trigger shortcut producer into the real composer, porting the three missing InputBar key behaviors, and arbitrating the two flutterTarget collisions (`component.ui-primitives.HoverCard`, `route.root`) under Migration Mode v1.1; target is Audited → Integrated where genuinely complete, never Verified.

Mounted `InputTriggerShortcuts` into `ConversationComposer` (`composer.dart`): the layer wraps the composer field inside the existing submit Shortcuts whenever `activatedRegistry` is bound, so Cmd/Ctrl+Z and Cmd/Ctrl+Shift+Z | Ctrl+Y drive the session controller's `ChipUndoStack` live — the React contract is InputBar.tsx lines 326–335 (`(e.metaKey || e.ctrlKey)` with redo on `y || shiftKey`, native stack suppressed because the machine owns the log); the platform split rides the tested `undoActivators`/`redoActivators` seam in `conversation_shortcuts.dart`.

Closed the orphaned-draft gap with a new `ComposerTriggerBinding` (`input_trigger/ui/composer_trigger_binding.dart`): controller.undo/redo alone mutated a draft copy no widget read (the old file doc deferred field sync to a future composer machine), so the binding pushes restored drafts back into the composer's `TextEditingController`, feeds every field edit through `controller.track` (chip transactions + trigger detection now run on live drafts), mirrors binding-written text into Riverpod state via an explicit commit callback, and ports `InputTriggerShortcuts.invocable` from the InputBar guard `if (machineBusy || locked) return` (keystroke consumed, mutation gated).

Ported chip Backspace/Delete occurrence deletion (InputBar.tsx lines 297–314) onto the binding's occurrence ledger: reference picks mint `{source, ref, offset, length, label, appearance?, clipboardText}` entries whose draft text is `'@' + label` (machine.ts `referenceDraftText`, `mint`, facade `insertReference`), Backspace removes the occurrence ending at a collapsed caret and Delete the one starting at it, edits reconcile the table exactly like machine.ts `reconcile` (wholly-before kept, at/after shifted by the delta, intersecting dropped), and a non-adjacent key returns ignored so native deletion proceeds — verified empirically that Flutter's TextField lets raw Backspace/Delete/Enter KeyDownEvents bubble to ancestor Focus nodes before DefaultTextEditingShortcuts acts.

Ported the Enter repeat guard (InputBar.tsx line 351 `if (e.repeat) return`) by making both Enter activators repeat-proof: plain Enter in `ConversationShortcuts` and the Cmd/Ctrl accelerated pair in the composer now use `SingleActivator(includeRepeats: false)`, so a held-down chord can neither machine-gun sends nor re-fire steering; Escape keeps its React-faithful unguarded behavior and Shift+Enter newline is untouched.

Ported empty-draft accelerated-Enter queue steering as detection + dispatch (InputBar.tsx lines 142, 350–362): with an empty draft and attachments, enabled input, a running session, and at least one `placement === 'queued'` row in the authoritative `queueProvider` snapshot, Ctrl/Cmd+Enter fires a new `composerQueueSteerHookProvider` instead of submitting; the effect half (`session.updateQueue({kind:'steer')}` per queued row, hub.ts `steerQueue`) needs a ConnectionClient RPC this slice does not own, so the hook stays inert until the connection workstream registers it, the `subagent === null` conjunct is documented as unrepresentable (`SessionSummary` carries no continuable-child marker), and the command-menu-open conjunct is covered upstream by the popup's own key capture.

Arbitrated `component.ui-primitives.HoverCard` / `dialog.hoverCard`: both rows audited the same single React source (`ui-primitives/src/HoverCard.tsx`) and Flutter realizes it in exactly one file, `widgets/primitives/hover_card.dart` (500 ms dwell aligned to the React `openDelayMs = 500` default, 100 ms grace across the 8 px gap — the component row's recorded "400 ms" was stale and is corrected); option (a) failed honestly because the only consumer surface, `features/sidebar/sidebar.dart`, is already the legitimate carrier of `screen.sidebar` and its `_WorkspaceHoverContent` / `_SessionHoverContent` are private classes there mirroring `ui-workspace` Rows.tsx, so the duplicate dialog row becomes `replacementType: not-applicable` citing the merge (the `api.llm` row precedent) and points at the related real export surface `widgets/primitives/primitives.dart` while the canonical component row owns the implementation file.

Arbitrated `route.root`: the honest Flutter carrier of AppFrame.tsx's own module contract ("registered into the built-in `'root'` slot — the web shell renders only 'root'") is `apps/flutter/lib/main.dart`, whose `DshApp` renders `SlotOutlet(slotKey: 'root')` as the only hardwired piece of the tree; the previously recorded target `routing/app_shell.dart` never existed on disk, and extracting either claimed file (`app_router.dart` from Verified `route.conversation.chat.node`, `app_frame.dart` from Verified `animation.dragHandle`) would be artificial churn against Verified rows, so route.root takes the unclaimed real bootstrap composition and advances to Integrated on its existing engineering evidence.

Tests added in `test/plugins/ws_input/input_trigger_shortcuts_test.dart` (9): undo/redo round-trip through the mounted layer on macOS (Cmd+Z / Cmd+Shift+Z) and Linux (Ctrl+Z / Ctrl+Y), the invocable gate consuming-but-not-mutating, occurrence mint/Delete-forward/Backspace/reconcile-over-append semantics, held-down Enter repeat suppression, steer dispatch gated on running+queued and cold for non-empty drafts, and an end-to-end mounted-composer test where typing `x /Foo`, picking the candidate, then pressing Backspace deletes the whole `@Foo` chip through the real sink path.

Verification: `flutter test test/plugins/ws_input` — 63/63 passed (all 39 pre-existing suite tests green plus this wave's additions; one transient cross-suite ordering flake in `approval_plane_test.dart` passed twice in isolation and on the full re-run); `flutter analyze lib` — 0 errors (sole remaining composer warning is the pre-existing unused `_ModelDropdown` element, untouched here).

Tracker result: `pnpm run verify-flutter-tracker --check` — OK (112 items) with unique flutterTargets throughout; `component.ui-primitives.HoverCard` and `route.root` advance Audited → Integrated with their evidence pointing at this report, `dialog.hoverCard` retires to not-applicable with the merge reason, and no Verified rows were modified.
