# Settings children, branding-adjacent forms, locale seam, clipboard — Agent D remediation 2026-08-25

Scope: four Audited rows — `screen.settings`, `form.language`, `form.locale`, `platform.clipboard` — classified implemented-but-missing-evidence before changes; two of them (`form.language`, `form.locale`) carried nonexistent flutterTarget paths now corrected to their real carriers; no row was promoted past Integrated and none was rewritten merely to change status.

Classification — screen.settings: implemented-but-missing-evidence; `apps/flutter/lib/src/features/settings/settings_screen.dart` exists as the four-tab settings host (General/Models/Plugins/Inventory via DefaultTabController) routed at `/settings` in `app_router.dart` line ~1304, mirroring the ui-settings/ui-settings-general shell split (base describe mirror + shell tabs) with the General section wired to LanguageRow/AppearanceRow and the scope-backed children plugins already Integrated; gap was zero focused coverage of the screen itself.

Classification — form.language: implemented-but-missing-evidence with wrong target; tracker claimed `apps/flutter/lib/src/features/settings/widgets/language_row.dart` (does not exist) while the real carrier `apps/flutter/lib/src/features/settings_general/widgets/language_row.dart` fully ports `packages/client/locale/src/client/LanguageRow.tsx` + `settings-store.ts` (LanguageRowState/LanguageRowController with revision-guarded sync, optimistic setLocale with rollback, DsSelect menu replacing React Menu with align-end portal, error SnackBar on failed mutate).

Classification — form.locale: implemented-but-missing-evidence with wrong target; tracker claimed `apps/flutter/lib/src/form/locale.dart` (does not exist); the React surface `LocaleRuntime` in `packages/client/locale/src/client/index.ts` maps to `LocaleService` inside `apps/flutter/lib/src/core/services/runtime_services.dart` (registry/bind face: per-namespace zh/en dictionaries, stable bound translate functions, fallback chain current → any registered locale carrying the key → key itself, revision bump on register/dispose, unknown-id rejection) with fifteen plugins registering dictionaries transactionally via `ctx.onDispose(locale.register(...))` from per-plugin `locales.dart` files and `'locale'` provided by `core/bootstrap/app_plugins.dart` and the ws_surfaces fixture; focused coverage already existed in `test/services/runtime_services_test.dart`.

Classification — platform.clipboard: implemented-but-missing-evidence; `apps/flutter/lib/src/platform/clipboard.dart` ports `packages/client/ui-primitives/src/clipboard.ts` onto Flutter's single `Clipboard` adapter (`Clipboard.setData` → navigator.clipboard.writeText on web / NSPasteboard on macOS, so the execCommand fallback becomes unnecessary), with empty-input short-circuit returning false without a channel call, denial caught to false, and `copyWithFeedback` SnackBar with platform-tuned failure copy (web HTTPS hint / macOS pasteboard message); consumed by `features/sidebar/sidebar.dart` and `features/conversation/widgets/message_list.dart` CopyButton.

Real defect found and fixed in owned files: `DsSelect`'s root `Column(crossAxisAlignment: stretch)` crashes with infinite-width constraints when placed directly inside a plain `Row` (Row lays out non-flex children width-unbounded), which broke both `language_row.dart` and the Enter-behavior row in `settings_screen.dart` at layout time — every other DsSelect call site hosts it inside a bounded stretch Column; fixed by wrapping the selector in `Flexible` + `ConstrainedBox(maxWidth: 200)` in both rows (content-hugging right-aligned pill, mirroring React's h36 r18 anchor), leaving the shared primitive `ds_select.dart` untouched because ui-primitives rows belong to Agent E.

New focused tests: `apps/flutter/test/features/settings_general/language_row_test.dart` (7 store/widget cases: initial en/zh options, stale-revision drop, describe parse of ns locale preference+revision, wire-down error arm, setLocale guard-describe → mutate → confirming re-sync sequence, optimistic rollback on mutate conflict, widget render of title + placeholder + synced label 中文); `apps/flutter/test/features/settings/settings_screen_test.dart` (6 cases over a canned `settings.describe` document: four tabs present, General renders Language row with synced English label + busy-Enter Queue message, notifications/workspace/About sections below the fold reached by scroll, Models tab provider editor intro, Plugins toggles list, Inventory table rows); `apps/flutter/test/platform/clipboard_test.dart` (6 cases with mocked `SystemChannels.platform`: setData receives exact text, empty input never touches the channel, PlatformException denial returns false, success/override SnackBars are floating with expected copy, macOS failure message under debugDefaultTargetPlatformOverride reset before the binding invariant check, top-level convenience delegates).

Tracker deltas applied: `screen.settings` flutterTarget unchanged (`features/settings/settings_screen.dart`) status Audited→Integrated; `form.language` flutterTarget corrected to `features/settings_general/widgets/language_row.dart` status Audited→Integrated; `form.locale` flutterTarget corrected to `core/services/runtime_services.dart` status Audited→Integrated; `platform.clipboard` target unchanged status Audited→Integrated; each gained owner `flutter-migration`, non-empty integrationPoints, real tests[] paths, evidence.testsRun naming the exact commands below and evidence.parityReport pointing at this file; BrandWordmark/FishLogo were NOT touched — they live in `widgets/primitives/` (pure ui-primitives, consumed by brand_official but not plugin-owned) and remain Agent E's rows.

Verification: `flutter analyze lib` 0 errors with zero findings in the touched files (during this window two concurrent agents' in-flight edits — agent_preset under Agent C and runtime_services.dart under Agent A — briefly introduced unrelated repo-wide errors; both landed before final certification); `flutter test test/platform/clipboard_test.dart test/features/settings_general/language_row_test.dart test/features/settings/settings_screen_test.dart test/plugins/ws_surfaces test/services/runtime_services_test.dart test/settings/settings_scope_test.dart test/goldens/surface_goldens_test.dart test/integration/business_host_test.dart` 48/48 including the 7 committed surface goldens and the real composed app host (`buildAppHost` + `PluginHost.activateAll`, five workstreams) as co-activation evidence; `pnpm run verify-flutter-tracker --check` OK (112 items) after the deltas.

Honesty notes / gaps remaining: no pixel goldens exist for the settings screen or language row (visual dimensions recorded as `partial`, not `pass`); web-side execution was not performed — platformParity.web recorded `partial` on compile-level reasoning (pure widget/Dart surfaces, no dart:io in these paths) rather than a live browser run, macos recorded `pass` for rows whose tests execute on the macOS host; `locale.service-dictionaries` (Agent A) still targets the nonexistent `core/locale/locale_service.dart` and will collide with `form.locale`'s corrected target if repointed at the same `runtime_services.dart` file — coordination recorded in `.agents/notes/proposed/process/2026-08-25-flutter-locale-row-targets.md`; BrandWordmark/FishLogo remain with Agent E untouched.
