# Brand Primitives / Runtime Services / Subagent Link Audited-Rows Completion — 2026-08-26

Scope: retry wave D completing four Audited rows — component.ui-primitives.BrandWordmark, component.ui-primitives.FishLogo, state.runtime, subagent.runtime-link. Target is Audited → Integrated under Migration Mode v1.1 where genuinely complete, honest gaps otherwise; nothing is promoted to Verified. English only.

## React contract extraction

FishLogo.tsx renders one filled path on native viewBox 23.16×17.04 at width `size` (default 24) with height `size * 17.04 / 23.16`; ink rides currentColor; the svg is aria-hidden decorative. BrandWordmark.tsx renders the figma 356:14644 extract on viewBox 182×24, or `26 0 156 24` with `includeMark=false`: nine "deepseek" letterform paths plus divider bar and chevron in currentColor, the whale path clipped to the rect at (0.1416, 3.5219, 23.16, 17.0435), a 52×14 rx2 badge plate, and seven HARNESS badge glyph paths filled `var(--dsw-alias-label-primary-inverted)`; aria-hidden throughout. The sole React consumer of both primitives is ui-brand-official's Brand.tsx, which registers OfficialBrandMark (FishLogo) into `sidebar.brand.mark` and `conversation.hero.brand.mark` and OfficialBrandName (BrandWordmark, includeMark=false) into `sidebar.brand.name`.

The React browser runtime plugin (packages/client/runtime/src/client/index.ts `apply()`) mounts SlotRegistry and the conversation registries, exposes SessionRuntime as `ctx.sessions` and WorkspaceRuntime as `ctx.workspaces`, and runs the connection-stream loop whose host-envelope bridge fans forwarded events out through `ctx.remote.$on`/`$dispatch`. ui-subagent's client `apply()` supplies openChild/refresh/setCatalogOpen callbacks calling `sessions.openSubagent` (manager.selectSubagent), `sessions.refreshSubagents`, and `sessions.setSubagentCatalogOpen`; child membership derives from session summaries (`origin === 'subagent'`, parentId walks via indexSubagentDescendants). React has no transcript-card click navigation — the session-header catalog action is the only nav leg into a child.

## Flutter state found

Both brand carriers already existed complete and faithful: fish_logo.dart carries the exact path data and ratio, brandwordmark.dart carries all seventeen fill paths verbatim with the React clip rects and inverted badge ink; the previous attempt left them analyzer-clean with passing focused tests. The prior attempt also recorded five theme goldens and fourteen geometry/widget assertions (brand_test.dart, brand_goldens_test.dart) that all pass; no implementation edits were needed. state.runtime's ledger target `lib/src/state/runtime.dart` never existed — the honest carrier is core/services/runtime_services.dart (SessionsService, WorkspacesService, LocaleService, re-exported RemoteEventBus), provided under the React service names by CoreServicesPlugin (`@core/runtime`) in app_plugins.dart; its test file pinned only LocaleService and RemoteEventBus. For subagent.runtime-link the carrier subagent_link.dart, its boot wiring in app_plugins.dart, the catalog action widget, the keyed node renderer, and ten focused tests already existed; what was missing was any widget-level proof of the composed catalog-click leg.

## Changes made by this wave

test/services/runtime_services_test.dart gains three groups pinning the mount-face contract against recording/hanging client fakes: SessionsService.history delegates the durable-window pull for exactly one requested id; WorkspacesService rides precisely `workspace.list`/`workspace.create`/`workspace.archiveSession` and `host.pickDirectory`/`host.listDirectory`/`host.createDirectory` including the nested-path fallback shape; DirectoryListSignal aborts pre-dispatch loudly without emitting a call and supersedes an in-flight scan so the caller frees immediately. runtime_services.dart receives one surgical lint cleanup (null-aware map element in listDirectory). test/plugins/ws_agent/subagent_plugin_test.dart adds two widget tests over the composed SubagentCatalogAction: picking a menu row navigates through the link and selects the child in the shared sessions list, and a session without child evidence renders no trigger (the React visibility rule). No file outside the four rows' ownership was touched.

## Per-row outcomes

Integrated (3): FishLogo — composed in three real surfaces (hero brand-mark slot via brand_official_plugin's slots.inject wait-and-follow, conversation hero empty-state, welcome-route hero) with exact geometry and goldens, so visual=pass is honest. state.runtime — flutterTarget corrected to the real carrier; every slice now proven at the wire-method level and consumed through the React service names by settings children, skill, agent-preset, plan, permission-presets, workspace, directory-picker, and input-trigger plugins. subagent.runtime-link — flutterTarget corrected to plugins/subagent/subagent_link.dart; navigation, catalog-open memory, refresh, keyed child-transcript rendering, and origin/parentId lineage are implemented, composed through session_header.dart's HoleOutlet, and tested down to the composed click; the read-only composer takeover remains deferred until the conversation hub declares its composer-chain seat, as documented in subagent_plugin.dart.

Migrated (1): BrandWordmark — artwork, both view windows, and inverted badge ink are faithful and golden-proven (visual=pass, behavior=pass), but its sole React composition `sidebar.brand.name` has no Dart counterpart because no plugin declares the sidebar brand holes yet, and the sidebar shell belongs to another workstream; following the 2026-08-25 Pill precedent, promotion waits for that composed leg rather than citing the unmounted BrandOfficial lockup as an integration point.

Evidence: `flutter analyze` scoped to all owned sources and tests (0 issues), `flutter test test/widgets/primitives/brand_test.dart test/goldens/brand_goldens_test.dart` (14 pass), `flutter test test/services/runtime_services_test.dart` (7 pass), `flutter test test/plugins/ws_agent/subagent_plugin_test.dart` (12 pass), and `pnpm run verify-flutter-tracker --check` clean after the ledger update.
