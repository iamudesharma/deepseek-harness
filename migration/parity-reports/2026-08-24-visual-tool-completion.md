Tool/visual parity completion — 2026-08-24
ToolNode now renders as a borderless 24px DisclosureRow (16 leading + title 14/24 + 2px sep dot + FILL ellipsis summary) with icon→chevron 100ms cross-fade gated by prefersReducedMotion, the whole row as expand toggle (expandOnRowClick + keepContentWhenOpen), and the expanded body in AnimatedSize (fast 100ms, zero when reduced) housing the unified ioCard (r12 borderL1 markdownCodeBlock, code-block-small 12/18, IN/OUT gutter grid with L2 divider, 150px capped sections, sticky caption label) or a typed card primitive (Terminal/Diff/Read/Search/Web) — the previous boxed bgLayer2 borderL2 radiusSm 560/640 single bubble container is removed, matching ToolRow.tsx + ToolRow.module.css geometry via DswAliases/DswTokens without literal colors.
Block-family geometry is now unified through DsBlockFrame (kBlockRadius 12, markdownCodeBlock surface, no consumer-owned border, clipAntiAlias) and shared head-tail cap kBlockCap 16 (ceil 8/8 slice) with BlockCopyButton (top-right 8/12, xs-13 secondary → copied 1s) and BlockExpandButton (full-width left, tertiary → secondary hover, “… 其余 N 行” / “收起”) — CodeBlock now has banner + copy at 12 radius, Diff/Read/Search/Web no longer carry radiusMd 8 + borderL2 mismatch and all cap at 16 with the same expand affordance, Terminal now correctly toggles expanded (was always false) and shares the same cap, fixing the 12 vs 8 hairline divergence.
Composer is now centered to the 748 measure (chat content width) with card = content+32 = 780 maxWidth, side clearance 16, floating r22 capsule (borderL2DarkmodeThin hairline, specificInputMajor fill, shadowLv2) and blue send disc (34 circle, buttonInfoFill #3964FE light / #679EFE dark, white arrow, 0.4 disabled) — the previous full-bleed docked bar with bgLayer1 top border and radiusLg 12 is replaced by the ConversationRoot → InputBar width coupling and InputBar.module.css floating capsule, verified via Center/ConstrainedBox 780 + Container radius 22.
AppFrame now freezes lastWideWidth while the 300ms slide (easeInOut) runs and cross-fades the wide content (150ms fade out via AnimatedOpacity, zero when reduced) before settling to the rail, the rail re-enters with a 150ms 49px slide+fade when everWide, the sidebar column tracks pointerInside and lingers the scrollbar thumb 2000ms via Theme scrollbarTheme transparent rebinding (quietBars), and the 36px rail controls plus the layout slide/handle transitions are gated by prefersReducedMotion (duration zero) beyond the existing modal gate — mirroring AppFrame.tsx (300ms slow + reduced-motion none, dragging none) and SidebarRoot.tsx (150 slide crossfade, freeze lastWideWidth, 2000ms linger, 36 rail).
Evidence: flutter analyze 0 errors (163 warnings, pre-existing), flutter test test/widgets/visual_tool_composer_test.dart (5 passed, covering bare+ioCard, unified block radius/cap/copy, 748/r22/disc, slide/crossfade/reduced, linger/rail), test/widgets/visual_parity_p7_test.dart (8 passed), test/widgets/primitives_test.dart, test/widgets/motion_test.dart, test/goldens/surface_goldens_test.dart (7 passed, chat light/dark, appframe expanded/collapsed, terminal ANSI, primitives row), test/widgets/app_frame_test.dart (6 passed), pnpm verify-flutter-tracker --check will be run and builds web/macos verified via flutter build.
Remaining gaps: subcall topology already Integrated but still flat in goldens (tool.subcall-topology UI leg tracked separately), composer hero vs docked phase coupling relies on ConversationRoot sticky/overlay bits not yet ported to Flutter column (hero glow/backdrop), trajectory/details overlay and workspace browsing region scrollbars are functional but not yet pixel-matched to WebBlock citation list maxHeight 320 vs 320 parity, and dark/light token parity for md-code-block small vs base is via aliases but Shiki highlighting still plain fallback.
