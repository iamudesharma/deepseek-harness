# UI-Primitives / Design-System Audited-Rows Remediation — 2026-08-25

Scope: fifteen Audited tracker rows owned by this remediation wave — component.ui-primitives.{Button, Input, JsonTree, Tooltip, ConnectionBanner, HoverCard, Markdown, ansi, head-tail-cap, icons, OnboardingSurface, Pill}, platform.web, route.root, route.ui-renderer. Target is Audited → Integrated under Migration Mode v1.1; Verified stays Gatekeeper-owned. English only.

## React contract extraction

Button.tsx: variants primary/ghost/outline/toolbar over --dsw-alias-button-* families, sizes md 36 / sm 28 capsules, optional leading 16px icon slot, native attributes pass through. Input.tsx: wrap span with gap 6, h36/h32, pad 0 8, r8, bgLayer1, borderL2, focus-within stateBusinessPrimary, optional leading 16px icon. Pill.tsx: static span vs interactive button when onClick present, .active adds ghost-active fill and border.

JsonTree.tsx: keyboard-accessible inspector tree with hover copy affordance (value/json/path/pretty menu), preview limits (object 4, array 5, depth 2), expandTopLevel, localized labels. Tooltip.tsx: dark token plate, side right/bottom/top, delayMs default 0, disabled flag, maxWidth cap. HoverCard.tsx: portal card at anchor right edge after 500ms dwell, pointer-grace so the card is reachable across an 8px gap, copy activation with 复制/复制成功 feedback. ConnectionBanner.tsx: single strip shown only while reconnecting, label via props. OnboardingSurface.tsx: body-portaled mask + opaque stage that sets #root inert for exactly its mounted lifetime.

MarkdownText.tsx: GFM through shiki highlighting, KaTeX math, file/session mentions, streaming-incremental parse, sanitizeUrl allowlist (http/https/mailto). ansi.ts: anser-based SGR runs folded to per-line spans, basic-color-to-theme-token mapping, OSC/non-CSI stripping, carriage-return/backspace/erase-in-line column replay with wide-char spacers, tab stops at 8. head-tail-cap.ts: pure split arithmetic — hidden = total − maxLines, headLines = ceil(maxLines/2), capped = hidden > 0 && !expanded. icons/index.tsx: custom outline SVG glyph set at fixed sizes with currentColor.

boot.ts: web boot kernel owning module table, Cordis loader, boot page, then handing the mount point to the ui-renderer entry. AppFrame.tsx: shell root composing sidebar | conversation | details | shell.overlay columns. ui-renderer/client: UiRenderer/App mount, SessionProvider, DocumentTitle, uSES adapter as the sole subscription seat.

## Flutter state found

Carriers existed for every row but several ledger paths had drifted to files that never existed (input.dart, jsontree.dart, tooltip.dart, connectionbanner.dart, hovercard.dart, onboardingsurface.dart, primitives/ansi.dart, primitives/head-tail-cap.dart, route/renderer.dart, routing/app_shell.dart, platform/web.dart); corrections below point each row at its real carrier. Button, Input, Tooltip, Pill already had focused widget coverage plus three button goldens and a pill row golden. JsonTree, ConnectionBanner, HoverCard, OnboardingSurface, icons, and Markdown's sanitize seam had no primitive-level tests. Two dead untracked stubs with wrong semantics sat in utils/: ansi.dart (strip-only AnsiParser) and head_tail_cap.dart (string truncation instead of line-cap metrics) — both deleted with zero references.

## Changes made by this wave

json_tree.dart gained IntrinsicWidth inside its horizontal scroll viewport: DisclosureRow returns a stretch-Column, which under the scroll view's infinite max width crashed layout for ANY composite root — the primitive could not render its primary case despite zero consumers exposing it. Tests now prove collapsed summaries, tap-to-expand, nested arrays, and typed leaf tinting. apps/flutter/test/widgets/primitives_remediation_test.dart adds 18 focused tests: banner state matrix, json-tree collapse semantics, hover-card delay/grace/disabled/copy wiring against a single mouse device, onboarding step flow and CTA swap, icon mapping, sanitizeUrl allowlist, gfm render, and link-tap inertness. No shared-file surfaces (sidebar, composer, chat_view, connection/session cores, block family) were edited.

## Per-row outcomes

Integrated (10): Button, Input, ConnectionBanner, Markdown, ansi, head-tail-cap, platform.web, route.ui-renderer — implementation + focused tests + production integration points recorded in the tracker. Markdown records visual=partial honestly: flutter_markdown carries no Shiki, KaTeX, mentions, or incremental parse; the sanitizeUrl seam is at full parity. ansi records behavior=partial: SGR foreground colors and bold only — cursor replay, wide-char columns, backgrounds, and 256/truecolor palettes are not reproduced, and its fixed palette stands in for React's theme-token color mapping. platform.web owns the WebSocket frame carrier seam; boot.ts's module-table/boot-page machinery is intentionally absent because AOT Flutter has no dynamic bundle loading, and reconnect/backoff parity belongs to the runtime.reconnect-generations row on connection_controller.dart.

Migrated (5): JsonTree, Tooltip, Pill, OnboardingSurface, icons — ported with focused tests but no composed-surface consumer yet (barrel exports or golden-only usage), so integration points cannot be claimed honestly. icons documents the deliberate Material-glyph substitution for the React custom SVG set. OnboardingSurface notes its superset step/CTA model versus React's chrome-only portal.

Held at Audited with complete evidence (2): component.ui-primitives.HoverCard and route.root are blocked purely by ledger collisions, not engineering gaps. HoverCard's real carrier hover_card.dart is flutterTarget-claimed by dialog.hoverCard, which duplicates this row's exact React source; route.root's two real carriers app_router.dart and app_frame.dart are claimed by Verified rows route.conversation.chat.node and animation.dragHandle respectively. Both held rows carry filled parity, tests, e2eScenarios, integrationPoints, evidence, and arbitration notes; Tracker Agent promotion follows target reconciliation.

## Verification

flutter analyze lib: my changed files contribute zero analyzer findings; the run reports 12 errors confined to lib/src/plugins/agent_preset/ui/agent_preset_screen.dart and lib/src/routing/app_router.dart from another workstream's concurrent provider rename, absent from this wave's baseline two hours earlier and expected to clear with that agent's completion. Test battery: flutter test test/widgets/primitives_test.dart test/widgets/primitives_p1_audit_test.dart test/widgets/primitives_remediation_test.dart test/widgets/visual_tool_composer_test.dart test/plugins/conversation_render_test.dart test/renderer/slot_outlet_test.dart test/goldens/surface_goldens_test.dart test/goldens/primitives_golden_test.dart test/unit/connection_client_ws_test.dart — 90 passing, including all goldens (button light/desktop/dark/ghost-narrow, terminal ANSI, primitives row, AppFrame pair). Tracker gate pnpm run verify-flutter-tracker --check re-run green after these edits.
