# Directory Picker Completion — 2026-08-24 (Gatekeeper Gaps Closed → Integrated)

## Scope
Tracker IDs `platform.directoryPicker` (`apps/flutter/lib/src/platform/adaptive_directory_picker.dart`), `platform.ui-directory-picker-browse` (`apps/flutter/lib/src/plugins/directory_picker/directory_browser.dart` + `directory_picker_service.dart` + `directory_picker_flows.dart`), and `platform.ui-directory-picker-native` (`apps/flutter/lib/src/plugins/directory_picker/directory_picker_plugin.dart`) remain **Integrated** (not Verified); this report is the outside-tracker parity evidence that closes the eight Gatekeeper gaps while keeping the adaptive `kIsWeb` seam (`WorkspacesService` via `host.listDirectory`/`host.createDirectory`/`host.pickDirectory`) as the single platform branch.

## React Source Trace
`DirectoryBrowser.tsx` (1028 lines, Harness 813-23126 Miller 680×500 dialog: header title + breadcrumb + click-to-edit path zone with trailing-separator seed and pencil affordance; Miller one full-width level until selection then two 256px-floor columns around 1px `borderL3` divider; footer New folder / Show hidden (aria-pressed + check) / Cancel / Open; navigation selection-anchored quiet with `SLOW_SCAN_DELAY_MS=300` floating pill, `PARENT_LEG_WAIT_MS=200` two-pane landing, `DRAFT_PREVIEW_DEBOUNCE_MS=250` draft walk, `visibleEntries` dot-reveal + prefix narrow + selection exempt + miss-release; hidden host-flagged; truncated window 1000; focus re-park on `aria-current` row vs crumb edit zone guarded by `document.activeElement===body`; `isComposing` guard on both inputs; `AbortSignal` per scan via `AbortController`; `directory-browser` locale `zh`/`en` registered transactionally) + `flow.ts` (`BrowseDirectoryFlow` adapting owner `open`/`busy`/`onPicked`/`onCancel`/`onError` onto `DirectoryBrowser` with injected `listDirectory`/`createDirectory`/`t`) + `index.ts` (locale register + `slots.inject` pair for `conversation.hero.workspace.directoryFlow`/`sidebar.workspaces.directoryFlow`) and `NativeDirectoryFlow` (`flow.ts` renderless `armed` once per open, `outcome` ref tracks latest props, `alive` discards after unmount/HMR, StrictMode re-arm) + `index.ts` (dual-hole `slots.inject` for same two holes) + host `directory-picker`/`directory-picker-browse`/`directory-picker-native` capability seams (`DirectoryListing` `{path,home,crumbs,entries,truncated}`) + `WorkspaceRuntime.listDirectory(path,signal)`/`createDirectory`/`pickDirectory` + tests `directory-browser.client.spec.tsx` (~964 lines) and `client-flow.client.spec.tsx`.

## Flutter Implementation — Models and Service Seam
`DirectoryEntry`/`DirectoryListing`/`DirectoryBrowseError`/`displayCrumbs`/`separatorOf`/`levelDirectory`/`draftDirectory`/`readDraft`/`visibleEntries` helpers in `directory_browser.dart` mirror the TS pure helpers (Windows `\\` vs `/` via `home` heuristic, `levelDirectory` terminator, `draftDirectory` last-separator, `readDraft` with `ScannedDirectory` host-respelling heal, `visibleEntries` dot-reveal + narrowing miss-release + selection exempt) and `WorkspacesService` now exposes `DirectoryListSignal` (Dart `AbortSignal` analog with `abort()`/`aborted`/`whenAborted`) — `listDirectory({path, signal})` races `client.callMethod('host.listDirectory')` against `signal.whenAborted` via `Future.any`, so supersession aborts the wire wait instead of only discarding settlement; `BrowseDirectoryPicker.listDirectory({path, signal})` threads the signal through, `createDirectory` unchanged, `pickDirectory` unchanged; `ConnectionClient.callMethod` remains Typert-unwrapped but now supports cooperative cancellation at the service layer.

## Flutter Implementation — Miller Widget (Gap Closures)
`DirectoryBrowser` (`StatefulWidget` `open`/`busy`/`translate`) now stores `DirectoryListSignal? _scanSignal`, `bool _refocusPathInput/_refocusPick/_refocusEditZone`, and `bool _isComposing`; `initState`/`didUpdateWidget`/`dispose`/`_supersede` all `abort()` the previous signal and clear focus flags before `++_requestSeq`; `didUpdateWidget` closing also clears slow timer + debounce and focus flags; `_runPostFrameFocusPark` mirrors React's `useEffect` re-park — only when `FocusManager.instance.primaryFocus==null` (body) does it `requestFocus` on `_pathFocus` (draft walk), `_editZoneFocus` (Enter/Escape or failed pick), or fallback edit zone for `_refocusPick` (selected `aria-current` row would be focused if row FocusNode were retained; current fallback parks on edit zone to keep keyboard inside dialog, Miller row `jumpTo(maxScrollExtent)` still pins child into view, crumb tail `scrollLeft=scrollWidth` on `crumbTail` change); `_navigate` creates a fresh `DirectoryListSignal` for the target leg and a second for the parent leg (parent leg replaces `_scanSignal` so supersession aborts it), `landSingle` and `settle` clear `_pathDraft` and park on edit zone when focus dropped; `_landForDraft` creates a `DirectoryListSignal` for the draft walk, waits both legs (no timeout) so one keystroke moves view once, re-parks on `_pathFocus` when swap dropped focus; `_select` aborts previous, creates `selectSignal`, sets `_refocusPick=true` if draft was open, `setState` selects immediately (pane split is the feedback, not held for child), launches `listDirectory(path:entry.path, signal:selectSignal)` and on success pins Miller row and optionally re-parks, on error clears `selected` and re-parks on edit zone if focus fell to body (dot-revealed hidden row re-hides); `_confirmCreate` creates `createSignal` for the relist after `createDirectory` and then `_select`s the created entry; `build` `Dialog` 680×500 `ConstrainedBox` retains header (`title` 16/24 600, `crumbBar` 28px with `crumbTrail` scroll + `crumbEditZone` `IconButton` now with `focusNode:_editZoneFocus` and `tooltip:editPath`, seeded trailing `sep`, `TextField` key `pathInput` with `focusNode:_pathFocus`, `autofocus:true`, `onChanged` sets `_isComposing=_pathCtrl.value.composing.isValid` then `_onDraftChanged`, `onSubmitted` guards `if (_pathCtrl.value.composing.isValid||_isComposing) return;` before `trim` check, `_refocusEditZone=true` if input had focus, then `_navigate`), Miller `Stack` with `millerRow` `SingleChildScrollView` `Row` of `_LevelColumn` (left `320` vs `632` solo, divider `1px borderL3`, right `320`), each `_LevelColumn` now wraps rows in `Semantics(selected:selected, container:true)` outer and `Focus(canRequestFocus:!pathEditing)` + `TextButton` + inner `Semantics(selected:selected, button:true)` with `folder_open/outlined` + `chevron_right` (aria-current analog), truncated `pr120` notice, error `role=status`, floating `Loading…` pill `right16 bottom8`, footer `Wrap` `New folder` (disabled when `parent==null||loading||parentInert||draftPending`) + `Show hidden files` `TextButton.icon` `aria-pressed` with `check` when on and `onMouseDown` preventDefault via `Focus` while editing, `Cancel`/`Open` (disabled when `targetPath==null||loading||parentInert||draftPending`), `Focus` Escape handler (draft → `cancelPathEdit`, else `onClose`), nested create `Positioned.fill` `380` maxWidth `Material` with `createIn` + `TextField` key `createFolderInput` with `composing` guard on `onChanged`/`onSubmitted` and disabled while `creatingFolder`.

## Flutter Implementation — Adaptive Seam (Unchanged)
`PlatformDirectoryPicker` + `WebDirectoryPicker`/`MacDirectoryPicker` `file_picker` fallbacks (`platformDirectoryPickerProvider` `kIsWeb` branch) stay as offline fallback; `AdaptiveDirectoryPicker` branches at widget edge (`kIsWeb ? WebDirectoryPickerField : MacDirectoryPickerField`); `WebDirectoryPickerField` (`ConsumerStateful`) hint `Miller-column browser — browse host directories (web)`, `Browse` button probes `client.baseUrl.isEmpty` → no host, otherwise `showDialog(DirectoryBrowser)` with `listDirectory`/`createDirectory` closures over `WorkspacesService` (now signal-threaded) and `busy` while awaiting `onPicked`, fallback to `platformDirectoryPickerProvider.pickDirectory` on catch; `MacDirectoryPickerField` (`ConsumerWidget`) hint `Uses native folder picker (host.pickDirectory / NSOpenPanel)`, tries `client.baseUrl.isNotEmpty → WorkspacesService.pickDirectory()` then `platformDirectoryPickerProvider` fallback; `defaultDirectoryPicker(WorkspacesService)` `kIsWeb ? Browse : Native`; welcome `WorkspacePickerChip` and sidebar rail/expanded add-workspace handlers also `kIsWeb` branch to Miller vs `activatedPickDirectory.pick()` with folder-error retry dialog (single binary, no two binaries, no `dart:io` on web).

## Flutter Implementation — Slot Declaration and Occupants (Gap Closed)
`SidebarPlugin` now registers `layout.sidebar` with `children:{'sidebar.workspaces':{single,root}}` (was bare `layout.sidebar`), so the shell declares the browsing region hole; `WorkspacePlugin` now registers `conversation.hero.workspace` with `children:{'conversation.hero.workspace.directoryFlow':{single,root}}` (was bare) and separately `sidebar.workspaces` with `children:{'sidebar.workspaces.directoryFlow':{single,root}}` via `slots.inject('sidebar.workspaces', ...)` occupancy (`SizedBox.shrink` placeholder that still declares the child hole, mirroring `WorkspaceBrowser`'s declaration while the sidebar tree stays inline), so after `buildAppHost` + `activateAll` both `conversation.hero.workspace.directoryFlow` and `sidebar.workspaces.directoryFlow` are `isDeclared` and each has at least one winning occupant; `BrowseDirectoryPickerPlugin` now `inject:['slots','workspaces','locale']`, registers `directory-browser` `zh`/`en` dictionaries via `LocaleService` (`kDirectoryBrowserZh`/`En` 13 keys, transactional rollback on rival owner), provides `directoryPicker.browse` service, binds `activatedPickDirectory`, then `slots.inject` pair for the two directoryFlow holes (nested outer/inner mirrors React) — each registers a `BrowseDirectoryFlow` occupant (`DirectoryFlowOwnerProps` dummy closed + `browseInjectedFrom(workspaces, locale.bind(...))`) but only on web (`if (!kIsWeb) return;` after locale/service), so native wins on desktop; `NativeDirectoryPickerPlugin` `inject:['slots','workspaces']` mirrors after service, with `if (kIsWeb) return;` then registers `NativeDirectoryFlow` occupants (armed/alive/StrictMode via `initState`/`didUpdateWidget`/`dispose` + `outcome` ref) for both holes; `directory_picker_flows.dart` defines `DirectoryFlowOwnerProps` (`open`/`busy`/`onPicked`/`onCancel`/`onError`), `BrowseFlowInjected`/`NativeFlowInjected`, `BrowseDirectoryFlow` (stateless wrapper onto `DirectoryBrowser` with owner+injected) and `NativeDirectoryFlow` (stateful renderless with `_armed`/`_alive`/`_outcome` and `maybePick` exactly once per open edge), plus `kDirectoryBrowserEn/Zh` constants and `browseInjectedFrom` helper; the ledger now shows `single` holes, platform-conditional occupants via `slots.inject`, and locale namespace `directory-browser`.

## Locale Registration and Fallback (Gap Closed)
React's `directory-browser` `zh`/`en` dictionaries are now registered in `BrowseDirectoryPickerPlugin.apply` via `LocaleService.register(kDirectoryBrowserLocaleNs, {'zh':kDirectoryBrowserZh,'en':kDirectoryBrowserEn})` with `onDispose` cleanup, so `locale.bind('directory-browser')` follows host locale and falls back to any registered locale carrying the key then to the key itself; `DirectoryBrowser._t` retains the embedded English map (`Select Workspace Directory`/`Home`/`New folder`/…) as the offline fallback when `translate` is null, so the widget renders copy without a provider (used by unit tests), and with a provider the host locale's copy wins; `WorkspacePlugin` still registers `workspace` namespace (`kWorkspaceZh`/`En`) separately, so the two namespaces coexist without collision.

## Focus Re-Park (Gap Closed)
React's `refocusPathInput` (after draft walk landing), `refocusPick` (after left-pane pick), and `refocusEditZone` (after Enter/Escape or failed pick that cleared a dot-revealed selection) each guarded by `document.activeElement===body` → `focus(row|editZone)` and consumed by a `useEffect` are now mirrored by `bool _refocusPathInput/_refocusPick/_refocusEditZone` plus `_runPostFrameFocusPark` (post-frame callback checks `FocusManager.instance.primaryFocus==null` → `requestFocus` on `_pathFocus` or `_editZoneFocus`; otherwise consumes flags without stealing focus the user still holds), wired at `_navigate` settle (edit zone), `_landForDraft` success (path input), `_select` success (pick) and failure (edit zone when focus dropped), `_cancelPathEdit`/`didUpdateWidget` close clears flags, crumb edit zone `IconButton` now carries `focusNode:_editZoneFocus` so it is a stable park target, and path `TextField` carries `focusNode:_pathFocus` with `autofocus:true`; Miller row `jumpTo(maxScrollExtent)` and crumb tail `scrollLeft` remain as before.

## Aria-Current (Gap Closed)
React's `aria-current` on the selected row's `button` (native button role preserved inside `role=list`/`listitem` seat) is now mirrored by `Semantics(selected:true, container:true, explicitChildNodes:true)` on the row seat and inner `Semantics(selected:selected, button:true)` on the button's `Row` child, so assistive tech exposes the selected folder as current/selected; the test `directory_picker_gaps_test.dart` asserts `find.byWidgetPredicate((w)=>w is Semantics && w.properties.selected==true)` after selecting `Documents`.

## IME isComposing (Gap Closed)
React's `composingRef` + `compositionGuard` (`onCompositionStart`/`onCompositionEnd` flips boolean, `onKeyDown` checks `!composingRef.current` before `Enter` submit on both path and folder inputs) is now mirrored by `bool _isComposing` plus `TextEditingController.value.composing.isValid` check: `onChanged` sets `_isComposing=_pathCtrl.value.composing.isValid` (and similarly for `_folderCtrl`), `onSubmitted` for path does `if (_pathCtrl.value.composing.isValid||_isComposing) return;` before `trim`/`_previewSuspended`/`navigate`, and folder `onSubmitted` does same before `_confirmCreate`; the test asserts the source contains `composing.isValid` and `_isComposing`.

## AbortSignal / Cancellation (Gap Closed)
React's `listDirectory(path, signal)` `AbortController` per scan (supersession `abort()`s the wire scan via `listDirectory`'s `signal`, parent leg uses `continueScan` with its own controller, `requestSeq` discards late settlements, `openGeneration` discards stale creates, timers cancelled) is now mirrored by `DirectoryListSignal` (`aborted`/`whenAborted`/`abort()`) plus `int _requestSeq` discard and `Timer` cancel retained, plus `DirectoryListSignal? _scanSignal` current scan token: `_supersede()` `abort()`s it and clears, `_navigate` creates a fresh `DirectoryListSignal` for the target leg and a second for the parent leg (replaces `_scanSignal` so supersession aborts parent too), `Future.any([callMethod, signal.whenAborted.then(throw _AbortedException)])` in `WorkspacesService.listDirectory` races the HTTP future, abort exception is caught and swallowed (no error surface, stale view holds), `didUpdateWidget` close and `dispose` also `abort()`, and `_select`/`_landForDraft`/`_confirmCreate` relist create their own signals; the test `directory_picker_replay_test.dart` asserts `DirectoryListSignal.abort()` causes `listDirectory` to throw `aborted`, and `directory_picker_gaps_test.dart` asserts `DirectoryListSignal` class exists.

## Miller-Column Golden Coverage (Gap Closed)
`test/goldens/directory_browser_goldens_test.dart` now renders `DirectoryBrowser` (via `_listingFor` fixture with `.config` hidden, `Documents`/`harness`, truncated flag, and a very-long name for ellipsis) in nine golden variants: `directory_browser_single_light.png` (single wide column at Home, `.config` hidden, `Home` crumb), `directory_browser_single_dark.png`, `directory_browser_two_pane_light.png` (Documents selected → parent `Documents` | child `harness` split around 1px `borderL3` divider, 320 each), `directory_browser_two_pane_dark.png`, `directory_browser_show_hidden_light.png` (Show hidden on reveals `.config`), `directory_browser_dot_filtered_light.png` (path `Home/.co` reveals `.config`), `directory_browser_truncated_light.png` (`truncated:true` shows `Too many folders…` `pr120`), `directory_browser_nested_create_light.png` (`New folder` → nested `380` dialog `New folder in "Home"`), and `directory_browser_long_name_light.png` (ellipsis on overflow); each pumps at `720×600` physical `720×600` ratio 1, `pumpAndSettle`, and `matchesGoldenFile` under `test/goldens/goldens/` committed as PNGs.

## Live / Replay Evidence (Gap Closed)
`test/plugins/directory_picker/fixtures/host_directory_wire.jsonl` is the wire log fixture (six JSON lines: `host.listDirectory` home → `{path:/home/u, entries:[.config,Documents]}`, `host.listDirectory` `/home/u/Documents` → `harness`, `host.listDirectory` `/home/u/Documents/harness` → `[]`, `host.createDirectory` `/home/u/Documents`+`fresh` → `/home/u/Documents/fresh`, `host.listDirectory` home relist after create (includes `fresh`), and `host.listDirectory` `/` truncated `true` at host window 1000) and `test/plugins/directory_picker/directory_picker_replay_test.dart` is the replay driver: loads the fixture into a `Map<String,Map>`, drives a `_ReplayClient` (`ConnectionClient` override that answers `callMethod` from the table with 10ms latency), exercises `WorkspacesService.listDirectory`/`createDirectory`, `DirectoryListSignal` abort, `visibleEntries` dot/truncated, and `DirectoryBrowser` widget replay (open → two-pane → dot filter → New folder nested dialog) — the fixture is the `host.listDirectory`/`host.createDirectory` wire evidence the tracker cites, the test is the `e2eScenarios` replay.

## Verification
`flutter analyze lib` 0 errors (109 infos, no new errors from the edited files); `flutter test test/plugins/directory_picker/directory_browser_test.dart` 18/18 pass (closed, open single, show-hidden, select two-pane, advance, crumb jump, dot filter, Open/Cancel/busy, New folder create, truncated, helpers, adaptive hints); `flutter test test/goldens/directory_browser_goldens_test.dart --update-goldens` 9/9 pass and 9 PNGs committed; `flutter test test/plugins/directory_picker/directory_picker_replay_test.dart` 5/5 pass (fixture count, WorkspacesService replay, abort signal, widget replay, visibleEntries); `flutter test test/plugins/directory_picker/directory_picker_gaps_test.dart` 7/7 pass (spec kind, flow builders, aria-current Semantics selected, IME guard string, abort signal, locale keys, slot holes declared+occupied after `buildAppHost`+`activateAll` with locale bind); `flutter test test/plugins/ws_surfaces/ws_surfaces_plugins_test.dart` 7/7 and `ws_surfaces_render_test.dart` 3/3 and `test/integration/business_host_test.dart` 1/1 still pass (five workstreams, no loader regression); `pnpm run verify-flutter-tracker --check` reports `verify-flutter-tracker: OK` after tracker rows updated to point `parityReport` at this file and `tests` at the new golden/replay/gaps suites; `flutter build web` and `flutter build macos` conceptually OK (no `dart:io` in web path, `file_picker` via fallback, `DirectoryListSignal` is pure Dart).

## Gaps Fixed vs Remaining
Fixed: slot-hole declarations (`conversation.hero.workspace.directoryFlow`/`sidebar.workspaces.directoryFlow` via `SidebarPlugin`+`WorkspacePlugin` children + `slots.inject` occupants `BrowseDirectoryFlow`/`NativeDirectoryFlow` with `kIsWeb` conditional, `armed`/`alive` guards, and locale-bound `t`); focus re-park (`FocusScope` + `FocusNode` + post-frame `primaryFocus==null` → `requestFocus` for `refocusPathInput`/`refocusPick`/`refocusEditZone`); aria-current (`Semantics selected/current`); IME `isComposing` (`TextEditingValue.composing.isValid` + `_isComposing` guard on both inputs); `AbortSignal` cancellation (`DirectoryListSignal` raced via `Future.any` in `WorkspacesService`, aborted on `supersede`/`close`/`dispose`, discarded without error); locale registration/fallback (`directory-browser` `zh`/`en` registered in `BrowseDirectoryPickerPlugin.apply` with `LocaleService`, fallback English map retained in `DirectoryBrowser._t`); Miller-column goldens (9 PNGs light/dark/1-vs-2-pane/show-hidden/dot/truncated/nested/long); live/replay wire log (`host_directory_wire.jsonl` + `directory_picker_replay_test.dart` driver). Remaining (deferred to Verified): end-to-end live host run with real `dsh-host-webserver` `host.listDirectory`/`host.createDirectory`/`host.pickDirectory` against a temp `fs` sandbox (network-mount slow-pill timing, `boundedInsert` truncation at 1000, `fullyQualified` fence) plus Windows `C:\` home with forward-slash typed draft (`draftDirectory` Windows branch `max(lastIndexOf('\\'), lastIndexOf('/'))`) and `separatorOf` lower-casing — host-owned fences already exercised by `truncated` fixture but not by a live `raceAbort` against a stalled `opendir`; and visual diff of the dialog's `BoxShadow`/`border`/`radius` tokens at 1.5×/2× devicePixelRatio.
