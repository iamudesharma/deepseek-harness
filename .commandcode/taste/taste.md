- Prefers web search in python_proxy.py exclusively via inbuilt Command Code / opencode endpoints via curl (Command Code ctx.web at $COMMAND_CODE_SEARCH_URL/web/search, opencode Go at $OPENCODE_SEARCH_URL/web/search or `opencode web search --json` binary) and explicitly wants DeepSeek search removed — no DEEPSEEK_API_KEY / web_search_20250305 / bespoke httpx fallback — mirroring how the app consumes Command Code in testing. Confidence: 0.95
- Prefers python_proxy.py web search to be self-contained inside the proxy and accessible directly from the API itself (POST /v1/web/search and /v1/search) via a built-in curl-based fallback when external inbuilt endpoints (127.0.0.1:5173 Command Code / 127.0.0.1:4096 opencode) are unavailable, mirroring the AI route at /v1/chat/completions — no reliance on separately running dev servers. Confidence: 0.9
